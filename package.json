{
  "name": "snake-game-fullstack",
  "version": "1.0.0",
  "description": "Snake Game with React, WebGL",
  "scripts": {
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "typescript": "^5.1.6"
  }
}
